import 'package:flutter/material.dart';

const double kDefaultMargin = 12.0;
const double kDefaultPadding = 12.0;
const double kDefaultIconSize = 24.0;
const double kDefaultFontSize = 18.0;
const double kAppbarRadius = 16.0;

const BorderRadius kDefaultBorderRadius =
    BorderRadius.all(Radius.circular(12.0));

const BorderRadius kDefaultButtonRadius =
    BorderRadius.all(Radius.circular(24.0));
